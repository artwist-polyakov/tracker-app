import Foundation
import UIKit

struct OnboardingData {
    
    /*
     title — заголовок, который будет выводиться в центре экрана
     buttonText — текст на кнопке
     imageName — картинки бывают двух видов OnboardingBackgroundRed или OnboardingBackgroundBlue
     */
    
    let title: String
    let buttonText: String
    let imageName: String
}

